# Release Notes

## v0.1.0-mvp

Highlights:

- Runnable system (`Lambda`, `Sequence`, `Parallel`, `Map`, `Passthrough`)
- Prompt and parser layers
- `ModelRunnable` and OpenAI provider integration
- Agent runtime API stabilization
- Core contract and changelog documentation

Refer to upstream `CHANGELOG.md` for full itemization.
